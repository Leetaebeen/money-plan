# Security policy

## 비밀정보 저장 원칙

- 실제 API 키, OAuth 비밀값, 인증 토큰, 개인키, 서비스 계정 파일은 Git에
  저장하지 않습니다.
- 로컬 개발 값은 `.env` 또는 `.dev.vars`에 두고, 배포 값은 GitHub Actions나
  배포 제공자의 암호화된 secret 저장소에 등록합니다.
- `.env.example`에는 변수 이름과 빈 값만 둡니다. 실제처럼 보이는 테스트 키도
  커밋하지 않습니다.
- `VITE_`, `NEXT_PUBLIC_`, `PUBLIC_` 접두사가 붙은 값은 브라우저에서 누구나
  볼 수 있으므로 비밀값을 넣지 않습니다.
- API 연동 소스 코드는 Git에서 관리하되 자격증명은 서버 전용 환경변수로
  주입합니다. 브라우저 앱은 비밀 API를 직접 호출하지 않고 서버 프록시를
  사용합니다.
- 사용자 급여, 부채, 계좌 및 투자 데이터의 원본·내보내기·백업 파일은 Git에
  저장하지 않습니다.

## 자동 보호 장치

- `.gitignore`가 로컬 환경 파일, 키·인증서, 서비스 계정, 개인 금융 데이터와
  로컬 데이터베이스를 제외합니다.
- pre-commit 훅이 스테이징된 파일명과 내용에서 알려진 비밀정보 패턴을 검사합니다.
- GitHub Actions가 전체 추적 파일과 새 커밋 메시지를 다시 검사합니다.

로컬 전체 검사는 다음 명령으로 실행합니다.

```bash
npm run security:check
```

## 노출 사고 대응

비밀값을 커밋했다면 파일만 삭제해서는 안 됩니다. 먼저 해당 제공자에서 키를
폐기하고 새 키로 교체한 다음, Git 이력 정리와 관련 로그 점검을 진행합니다.
공개 이력 재작성이나 강제 푸시는 협업자의 작업을 망가뜨릴 수 있으므로 별도
승인과 백업 없이 수행하지 않습니다.
